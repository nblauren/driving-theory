import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Backgrounds ──────────────────────────────────────────────
  static const backgroundPrimary = Color(0xFF09090F);
  static const backgroundCard = Color(0xFF0F0F1A);
  static const backgroundElevated = Color(0xFF141422);
  static const backgroundSubtle = Color(0x0AFFFFFF); // rgba(255,255,255,0.04)

  // ── Electric Blue Accents ────────────────────────────────────
  static const accentPrimary = Color(0xFF0066FF);
  static const accentLight = Color(0xFF4DA3FF);
  static const accentGlow = Color(0x260064FF); // rgba(0,100,255,0.15)

  // ── Semantic ─────────────────────────────────────────────────
  static const success = Color(0xFF2ECC71);
  static const successSubtle = Color(0x1F2ECC71); // rgba(46,204,113,0.12)
  static const error = Color(0xFFE74C3C);
  static const errorSubtle = Color(0x1AE74C3C); // rgba(231,76,60,0.10)
  static const warning = Color(0xFFF5A623);
  static const warningSubtle = Color(0x1FF5A623); // rgba(245,166,35,0.12)

  // ── Text ─────────────────────────────────────────────────────
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0x8CFFFFFF); // rgba(255,255,255,0.55)
  static const textTertiary = Color(0x4DFFFFFF); // rgba(255,255,255,0.30)

  // ── Borders ──────────────────────────────────────────────────
  static const borderSubtle = Color(0x12FFFFFF); // rgba(255,255,255,0.07)
  static const borderMedium = Color(0x1FFFFFFF); // rgba(255,255,255,0.12)
  static const borderAccent = Color(0x4D0078FF); // rgba(0,120,255,0.30)

  // ── Light Mode ───────────────────────────────────────────────
  static const lightBackgroundPrimary = Color(0xFFF5F5F7);
  static const lightBackgroundCard = Color(0xFFFFFFFF);
  static const lightBackgroundElevated = Color(0xFFEBEBF0);
  static const lightBackgroundSubtle = Color(0x0A000000);
  static const lightTextPrimary = Color(0xFF0A0A14);
  static const lightTextSecondary = Color(0x80000000); // rgba(0,0,0,0.50)
  static const lightTextTertiary = Color(0x4D000000); // rgba(0,0,0,0.30)
  static const lightBorderSubtle = Color(0x12000000); // rgba(0,0,0,0.07)
  static const lightBorderMedium = Color(0x1F000000); // rgba(0,0,0,0.12)
}
