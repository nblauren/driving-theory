/// The current consent version. Bump this number whenever the privacy policy
/// or terms of use change materially — the consent gate will reappear for all
/// users who accepted a lower version.
const int currentConsentVersion = 1;
